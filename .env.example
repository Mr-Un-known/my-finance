# Fase 13 en adelante. En Fase 1-12 la app funciona 100% local (IndexedDB).
# La anon key es publica por diseno; lo que protege los datos es RLS + Auth.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Fase 14. Clave publica VAPID (la privada NUNCA va aqui, va en secrets de Supabase).
VITE_VAPID_PUBLIC_KEY=
